# 数据管理

出于科研保密、可追溯性和仓库体积控制，本目录默认不跟踪真实数据文件。

- `data/sample/`：仅放少量、脱敏、可复现实例。
- 原始数据建议保存在本地 `data/raw/`。
- 清洗后的内部数据建议保存在本地 `data/processed/`。
- 外部数据建议保存在本地 `data/external/`。

上述本地目录均受 `.gitignore` 保护。不要通过修改文件名绕过保护规则。

