# 配置文件

- `prediction_condition.example.json`：单条新工况的字段和格式示例。
- `calibration_three_point.example.csv`：低/中/高三个虚构校准点的格式示例。
- `calibrated_condition.example.json`：位于三点校准范围内的同日预测工况示例。

示例使用 2099 年虚构日期和确定性生成值，只用于展示文件格式，不是实验结果。

密码、Token、数据库连接串、现场精确位置和污水厂敏感信息不得写入此目录。
