{
  "name": "waterpilot-miniprogram",
  "version": "0.1.0",
  "private": true,
  "description": "OUR-informed SND decision-support WeChat Mini Program",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:js": "find . -name '*.js' -not -path './node_modules/*' -print0 | xargs -0 -n1 node --check",
    "check:json": "node scripts/check-json.js",
    "check": "npm run check:js && npm run check:json && npm test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
