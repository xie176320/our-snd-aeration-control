# 安全策略

## 支持范围

当前仅维护 `main` 分支的最新版本。本项目是科研决策支持原型，不是安全认证的工业控制系统。

## 报告漏洞

请不要在公开 Issue 中披露可被利用的漏洞、真实数据或现场连接信息。请通过维护者的 GitHub 主页发起私下联系，并提供：

- 受影响版本与文件；
- 可复现步骤；
- 影响范围；
- 建议修复方式（如有）。

维护者会先确认报告，再评估修复与披露时间。

## 部署边界

- 公共演示只应使用 `data/sample/` 中的合成数据。
- 不要把模型服务直接连接 PLC、曝气设备或生产数据库。
- 不要加载来源不可信的 `.joblib`/pickle 模型文件。
- 生产化前必须增加身份认证、传输加密、审计日志、人工确认和故障回退。
