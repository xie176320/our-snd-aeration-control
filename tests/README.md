# 测试

当前测试覆盖：

- Excel 序列日期与 `M.D` 日期规范化；
- `6.2` 这类歧义日期由完整日期工作表解析；
- 标准训练字段、范围、记录数和日期组数；
- 不完整记录可被逐行审计，且在剩余有效样本足够时不阻断训练；
- 兼容分组拆分不会让同一日期跨训练/验证集。
- 当日校准点覆盖互不相同的低/中/高曝气水平，且残差校准计算可复现。
- 网页演示模型复用日期分组验证，预测值保持在物理范围内。
- 曝气响应曲线不越出历史曝气范围，非法工况会被拒绝。

运行：`python -m unittest discover -s tests -v`。
